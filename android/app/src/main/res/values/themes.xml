<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the whole UI; the platform theme only needs to stay out of the way. -->
    <style name="Theme.Murmur" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
