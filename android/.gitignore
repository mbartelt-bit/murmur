# Local Android SDK location (see README.md; ANDROID_HOME works too)
local.properties

# Gradle / build outputs
.gradle/
build/
.kotlin/

# IDE
*.iml
.idea/

# Release signing — the upload keystore and its passwords never go in the repo
# (see keystore.properties.example).
keystore.properties
*.jks
*.keystore
